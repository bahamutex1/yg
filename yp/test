{
  "spider": "./jar/pg.jar;md5;xxxxxxxxxxxx", 
  // 解析插件 (通常是 jar 檔)，後面加 md5 校驗碼

  "logo": "https://example.com/logo.png", 
  // 應用程式的圖示或封面圖片

  "lives": [
    {
      "name": "測試直播源1",   // 直播源名稱
      "type": 0,              // 類型 (通常固定為 0)
      "url": "./list.m3u"     // 直播清單檔案位置 (m3u 或 txt)
    },
    {
      "name": "測試直播源2",
      "type": 0,
      "url": "https://example.com/live.txt", // 另一個直播清單
      "playerType": 1,                       // 播放器類型 (可選)
      "epg": "http://epg.example.com/?ch={name}&date={date}", // 電子節目表
      "logo": "https://example.com/logo/{name}.png",          // 該源的圖示
      "pass": true                           // 是否需要驗證 (true/false)
    }
  ],

  "wallpaper": "https://example.com/wallpaper.jpg", 
  // 背景牆紙圖片

  "sites": [
    {
      "key": "search_api",       // 唯一識別碼
      "name": "搜尋站",          // 顯示名稱
      "type": 3,                 // 類型 (3 表示 JS/插件型)
      "api": "./lib/search.js",  // 對應的 API 或 JS 檔案
      "searchable": 1,           // 是否支援搜尋 (1=是, 0=否)
      "quickSearch": 1,          // 是否支援快速搜尋
      "filterable": 1            // 是否支援篩選
    },
    {
      "key": "video_api",
      "name": "影片站",
      "type": 3,
      "api": "csp_Video",        // 另一種 API 型態
      "searchable": 1,
      "quickSearch": 0,
      "filterable": 0,
      "style": { "type": "rect", "ratio": 1.77 } // 顯示樣式 (矩形比例)
    }
  ],

  "rules": [
    {
      "name": "proxy", 
      // 規則名稱
      "hosts": ["example.com", "test.com"], 
      // 要過濾的主機
      "regex": ["廣告", "VIP專區"] 
      // 要過濾的文字或正則表達式
    }
  ]
}
